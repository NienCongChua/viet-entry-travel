# Base URL of the backend API (see backend/README.md) used by the
# "Request a Quote" form to email the team.
# Local dev default matches `npm run dev` in backend/.
VITE_API_URL=http://localhost:4000
